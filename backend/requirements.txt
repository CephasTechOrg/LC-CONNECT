# Production dependencies — pinned for reproducible builds (CI, Render, local).
# To upgrade: bump a version here, reinstall, run `pytest`, and confirm the OpenAPI
# snapshot still passes (only UPDATE_SNAPSHOTS=1 if the API is meant to change).
fastapi==0.139.2
uvicorn[standard]==0.51.0
SQLAlchemy[asyncio]==2.0.51
asyncpg==0.31.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-jose[cryptography]==3.5.0
PyJWT[crypto]==2.13.0
httpx==0.28.1
passlib[bcrypt]==1.7.4
bcrypt==4.3.0
python-multipart==0.0.32
supabase==2.31.0
email-validator==2.3.0
orjson==3.11.9
resend==2.34.0
alembic==1.18.5
